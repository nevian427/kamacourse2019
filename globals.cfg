debug=2   // уровень debug
log_stderror=no  // не писать лог в stderr

memdbg=5  // уровень дебага памяти
memlog=5  // уровень лога памяти
 
log_facility=LOG_LOCAL0 // Писать лог в канал rsyslog 0

children=2 // количество подпроцессов

disable_tcp=no // разрешить TCP

mpath="/usr/local/lib64/kamailio/modules/" //из какой дирректории брать модули

mhomed=1
